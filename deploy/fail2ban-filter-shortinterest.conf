# fail2ban filter for Short Interest Tracker
# Copy to /etc/fail2ban/filter.d/shortinterest.conf
#
# Catches 401 responses (failed Basic Auth) in Apache combined log format

[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD) .* HTTP/.*" 401
ignoreregex =
